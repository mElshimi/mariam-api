{
  "scripts": {
    "start": "json-server-auth --watch db.json --port 4000"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0",
    "multer": "^1.4.5-lts.1"
  }
}
